# npm 常用命令

```bash
1.npm get registry  # --显示当前的镜像网址

  https://registry.npmjs.org/

2. # 使用淘宝镜像  npm config set registry +淘宝镜像

npm config set registry http://registry.npm.taobao.org  

3.  # 配置后可通过下面方式来验证是否成功

npm config get registry 或 npm info express
```

